export enum LANG {
  EN = "en",
  ID = "id",
}

export const LANGS = [LANG.EN, LANG.ID];

export enum THEME {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export const THEMES = Object.values(THEME);
